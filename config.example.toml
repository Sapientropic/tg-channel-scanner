# Telegram API credentials
# Get yours at https://my.telegram.org/apps
# If the form shows ERROR, see docs/getting-api-credentials.md

api_id = 0
api_hash = "your_api_hash_here"
# scan.py stores the Telethon session at ~/.config/tgcli/session by default.
# Set TG_SCANNER_CONFIG_DIR to keep config and session files somewhere else.
